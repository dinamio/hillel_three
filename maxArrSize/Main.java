package main.java;

public class Main {

    public static void main(String[] args) {

        Array userArray = new Array();

        userArray.getUserArraySize();
        userArray.fillArray();
        userArray.findMaxArrValue(userArray.getArray());
    }
}